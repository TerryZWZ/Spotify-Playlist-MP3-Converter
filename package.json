{
  "scripts": {
    "dev": "cd server && npm run dev",
    "vercel-build": "cd server && npm i vite && chmod a+rx node_modules/.bin/concurrently && chmod a+rx node_modules/.bin/nodemon && npm run dev"
  }
}